# Codex Remote

Recovered workspace snapshot for the Codex Remote monorepo.

This local restore recreates the base pnpm workspace, key gateway/protocol/mobile-web paths, and the mobile-web source files that were recoverable from local session logs.
