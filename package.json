{
  "name": "codex-remote",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.6.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "test": "pnpm -r test",
    "stack:prepare-host": "./scripts/configure-host-network.sh",
    "stack:start:gateway": "./scripts/start-gateway.sh",
    "stack:start:web": "./scripts/start-mobile-web.sh",
    "stack:publish": "./scripts/publish-tailscale.sh",
    "stack:check": "./scripts/check-phone-stack.sh"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsup": "^8.4.0",
    "typescript": "^5.8.2",
    "vitest": "^3.0.8"
  }
}
